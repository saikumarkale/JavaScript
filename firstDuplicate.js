/**
 * take user input array
 * 
 * 
 * check for edge cases:{
 *                    length of array is >1
 *                     !input
 *                     }
 * create new empty array
 * pick the [0]element from the arrey and add in to the new array
 * itrate the process
 * if element is not present inside the new array then add the element 
 * continue untill the element is present inside the new array
 * print the element and return
 * 
 */